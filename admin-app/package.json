{
  "name": "golist-admin-app-service",
  "version": "2.5.0",
  "description": "Quickly browse a list of links and files with one simple URL",
  "scripts": {
    "server": "nodemon ./bin/www",
    "client": "npm start --prefix client",
    "concurrent": "concurrently 'npm run server' 'npm run client'",
    "build": "npm run-script build --prefix client && mv client/build .",
    "start": "PORT=8080 node ./bin/www",
    "dev": "npm run client",
    "preversion": "npm --no-git-tag-version version minor --prefix client",
    "bump-version": "npm version minor -m 'bump version to %s for deployment'",
    "deploy": "gcloud app deploy",
    "deploy-new": "npm run bump-version && gcloud app deploy",
    "test": "npm run build && echo 'build successful'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Michael-Tu/GoList.git"
  },
  "author": "Gan Tu",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/Michael-Tu/GoList/issues"
  },
  "homepage": "https://github.com/Michael-Tu/GoList#readme",
  "dependencies": {
    "@google-cloud/datastore": "^6.3.1",
    "body-parser": "^1.19.0",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "express-rate-limit": "^5.2.3",
    "http-errors": "~1.6.3",
    "morgan": "~1.9.1",
    "pug": "^3.0.0"
  },
  "devDependencies": {
    "client": "file:./client",
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.7"
  }
}
